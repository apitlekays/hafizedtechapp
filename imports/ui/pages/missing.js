import React from 'react'

const Missing = (props) => {
    return (
        <p>This is the missing page. When you typed in an invalid URL, you will go straight here. Go back home and cry.</p>
    )
}
export default Missing;